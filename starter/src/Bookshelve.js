const Bookshelve = ({category}) => {

    return (
        <div className="bookshelf">
            <div className="bookshelf-title">{category}</div>
        </div>
    )
}

export default Bookshelve;
